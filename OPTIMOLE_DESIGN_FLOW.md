Optimole:

-User Agent:
-Recon Agent:
-Analyst Agent:
-System Agent:
-CleanUp Agent

Optimole, should scan the users system to retrieve system specs, memory, storage, cpu/gpu. Then Get the OS memory and storage usage
Get all apps and get the memory and storage usage of each app.
Then get the memory information for folders, files, downloads, etc.

Recon Agent: Should research and discover methods to optimize the users usage. Then send the results back to the system agent.(Usage Agent).

System Agent: Should implement the suggestions in a reasonable order from item with highest possible impact down to lowest.(Analyst Agent)

Clean Up agent : Should reevaluate current system and its memory/storage space to pass on to Usage Agent.