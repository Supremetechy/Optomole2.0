# Spec Engine

JSON schemas for Optimole-generated game specifications live here.

Current migrated schemas:

- `schemas/experience-manifest.schema.json`
- `schemas/game-definition.schema.json`
- `schemas/game-design-package.schema.json`

API and worker validation should depend on these schemas instead of maintaining separate contract copies.
